 --duplicate and null values

 select
 prd_id ,count(*) as prd_id
 from bronze.crm_prd_info
 group by  prd_id
 having count(*) >1 or prd_id is null;

  --we want to check if we have any products that are not included in order
  
   select *, 
  replace(Substring(prd_key,1,5),'-','_') as cat_id,
 substring(prd_key,7,len(prd_key)) as prd_key
 from bronze.crm_prd_info
 where  substring(prd_key,7,len(prd_key)) not in (
 select sls_prd_key from bronze.crm_sales_details);

  --check unwanted space prd_nm

 select prd_nm
from bronze.crm_prd_info
where prd_nm !=trim(prd_nm)

--check for null or negative vlaue

select prd_cost from bronze.crm_prd_info
where prd_cost < 0 or prd_cost is null;

 --check for value where end date is less then start date
 select * from bronze.crm_prd_info
 where prd_end_dt < prd_start_dt;








